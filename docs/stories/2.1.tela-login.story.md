# Story 2.1: Tela de Login Simplificado

## Status
Draft

## Executor Assignment
```
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["lint", "typecheck", "build"]
```

## Story
**As a** paciente da rede de clínicas,
**I want** me identificar no sistema inserindo apenas meu e-mail,
**so that** possa acessar o concierge de saúde de forma rápida e sem fricção.

## Acceptance Criteria

1. A rota `/` redireciona automaticamente para `/login`
2. A página `/login` exibe um campo de e-mail e um botão de acesso
3. O campo de e-mail valida o formato básico antes de permitir o envio (HTML5 `type="email"`)
4. Ao submeter o formulário com e-mail válido, o sistema salva os dados do paciente em `sessionStorage` usando `src/lib/session.ts`
5. Após salvar a sessão, o usuário é redirecionado para `/chat`
6. Se o campo estiver vazio ou inválido, o botão permanece desabilitado ou exibe mensagem de erro
7. O layout é adequado para usuários idosos: fonte grande (mínimo 18px no campo e botão), botão grande e com alto contraste
8. A página é responsiva (funciona bem em mobile e desktop)

## Tasks / Subtasks

- [ ] Criar redirecionamento da raiz para `/login` (AC: 1)
  - [ ] Editar `src/app/page.tsx` para fazer redirect para `/login`
- [ ] Criar página de login `src/app/login/page.tsx` (AC: 2, 3, 4, 5, 6)
  - [ ] Componente com campo de e-mail controlado (`useState`)
  - [ ] Validação de e-mail via regex simples antes de habilitar o botão
  - [ ] Handler de submit: salvar sessão + redirect para `/chat`
  - [ ] Usar `useRouter()` do Next.js para redirect
- [ ] Usar `src/lib/session.ts` para persistir sessão (AC: 4)
  - [ ] Salvar `{ patientId: email, patientName: email, email }` no `sessionStorage`
- [ ] Aplicar estilos acessíveis com Tailwind (AC: 7, 8)
  - [ ] Fonte ≥ `text-lg` (18px) em campos e botão
  - [ ] Botão com altura ≥ 48px (`py-3` ou superior)
  - [ ] Cores de alto contraste (fundo escuro no botão, texto branco)
  - [ ] Layout centralizado verticalmente na tela
- [ ] Criar layout da aplicação `src/app/layout.tsx` com metadados do produto (AC: 8)

## Dev Notes

### Estrutura do componente — `src/app/login/page.tsx`

```tsx
'use client';

import { useState } from 'react';
import { useRouter } from 'next/navigation';
import { saveSession } from '@/lib/session';

export default function LoginPage() {
  const [email, setEmail] = useState('');
  const [error, setError] = useState('');
  const router = useRouter();

  const isValidEmail = (v: string) => /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(v);

  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault();
    if (!isValidEmail(email)) {
      setError('Por favor, insira um e-mail válido.');
      return;
    }
    saveSession({ patientId: email, patientName: email, email });
    router.push('/chat');
  };

  return (
    <main className="min-h-screen flex items-center justify-center bg-blue-50">
      <div className="bg-white rounded-2xl shadow-lg p-10 w-full max-w-md">
        <h1 className="text-3xl font-bold text-blue-800 mb-2 text-center">
          Health Concierge
        </h1>
        <p className="text-gray-500 text-center mb-8 text-lg">
          Seu assistente de saúde
        </p>
        <form onSubmit={handleSubmit} className="flex flex-col gap-4">
          <label htmlFor="email" className="text-lg font-medium text-gray-700">
            E-mail
          </label>
          <input
            id="email"
            type="email"
            value={email}
            onChange={e => { setEmail(e.target.value); setError(''); }}
            placeholder="seu@email.com"
            className="border border-gray-300 rounded-xl px-4 py-3 text-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
            required
          />
          {error && <p className="text-red-500 text-sm">{error}</p>}
          <button
            type="submit"
            disabled={!email}
            className="bg-blue-700 hover:bg-blue-800 disabled:opacity-50 text-white font-semibold rounded-xl py-4 text-xl mt-2 transition-colors"
          >
            Acessar
          </button>
        </form>
      </div>
    </main>
  );
}
```

### Redirecionamento da raiz — `src/app/page.tsx`

```tsx
import { redirect } from 'next/navigation';

export default function RootPage() {
  redirect('/login');
}
```

### Proteção de rota `/chat`
A story 3.1 será responsável por proteger `/chat` contra acesso sem sessão ativa.

### Referências
- `docs/architecture.md` — Fluxo de Login, dados de sessão, `src/lib/session.ts`
- `docs/project-brief.md` — Tela 1 (Login), CON-05 (sem senha no MVP)

### Dependência
- Story 1.1 deve estar concluída (setup do projeto e `src/lib/session.ts` criados)

## Testing
- Testar manualmente: campo vazio → botão desabilitado; e-mail inválido → erro; e-mail válido → redirect para `/chat`
- Verificar `sessionStorage` no DevTools após login

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-11 | 1.0.0 | Story criada | River (@sm) |
