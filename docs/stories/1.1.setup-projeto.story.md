# Story 1.1: Setup do Projeto Next.js

## Status
Draft

## Executor Assignment
```
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["lint", "typecheck", "build"]
```

## Story
**As a** desenvolvedor,
**I want** um projeto Next.js devidamente configurado com TypeScript, Tailwind CSS e variáveis de ambiente,
**so that** todas as outras stories possam ser implementadas sobre uma base sólida e padronizada.

## Acceptance Criteria

1. Projeto Next.js 14+ criado com App Router e TypeScript habilitado
2. Tailwind CSS instalado e configurado
3. Arquivo `.env.local` criado com `NEXT_PUBLIC_API_BASE_URL=http://localhost:5000`
4. Arquivo `.env.production` criado com placeholder `NEXT_PUBLIC_API_BASE_URL=https://REPLACE_WITH_AZURE_URL`
5. Estrutura de pastas criada conforme arquitetura definida em `docs/architecture.md`
6. Arquivo `src/types/index.ts` criado com os tipos base do projeto
7. Arquivo `src/services/api.ts` criado com as funções de chamada HTTP
8. `npm run build` executa sem erros
9. `npm run lint` executa sem erros

## Tasks / Subtasks

- [ ] Inicializar projeto Next.js com TypeScript e App Router (AC: 1)
  - [ ] `npx create-next-app@latest . --typescript --tailwind --app --src-dir --import-alias "@/*"`
- [ ] Configurar Tailwind CSS (AC: 2)
  - [ ] Verificar que `tailwind.config.ts` inclui o path `./src/**/*.{ts,tsx}`
- [ ] Criar arquivos de ambiente (AC: 3, 4)
  - [ ] Criar `.env.local` com `NEXT_PUBLIC_API_BASE_URL=http://localhost:5000`
  - [ ] Criar `.env.production` com placeholder da URL do Azure
  - [ ] Adicionar `.env.local` ao `.gitignore`
- [ ] Criar estrutura de pastas (AC: 5)
  - [ ] `src/app/login/` 
  - [ ] `src/app/chat/`
  - [ ] `src/components/ChatInterface/`
  - [ ] `src/components/MessageBubble/`
  - [ ] `src/components/VoiceButton/`
  - [ ] `src/components/TextInput/`
  - [ ] `src/hooks/`
  - [ ] `src/services/`
  - [ ] `src/types/`
  - [ ] `src/lib/`
- [ ] Criar `src/types/index.ts` com tipos base (AC: 6)
- [ ] Criar `src/services/api.ts` com funções HTTP (AC: 7)
- [ ] Validar build e lint (AC: 8, 9)

## Dev Notes

### Tipos base — `src/types/index.ts`

```typescript
export interface Message {
  role: 'user' | 'assistant';
  content: string;
  timestamp: number;
  type: 'text' | 'voice';
}

export interface InteractRequest {
  Prompt: string;
  JsonHistory?: string;
  PatientId?: string;
  PatientName?: string;
}

export interface InteractResponse {
  resposta: string;
}

export interface PatientSession {
  patientId: string;
  patientName: string;
  email: string;
}
```

### Serviço HTTP — `src/services/api.ts`

```typescript
const API_BASE = process.env.NEXT_PUBLIC_API_BASE_URL;

export async function sendTextMessage(
  prompt: string,
  history: Message[],
  session: PatientSession
): Promise<InteractResponse> {
  const payload: InteractRequest = {
    Prompt: prompt,
    JsonHistory: JSON.stringify(
      history.map(m => ({ role: m.role, content: m.content }))
    ),
    PatientId: session.patientId,
    PatientName: session.patientName,
  };

  const res = await fetch(`${API_BASE}/api/atendimento/interagir`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(payload),
  });

  if (!res.ok) throw new Error(`API error: ${res.status}`);
  return res.json();
}

export async function sendVoiceMessage(
  wavFile: File,
  history: Message[],
  session: PatientSession
): Promise<string> {
  const formData = new FormData();
  formData.append('file', wavFile, 'audio.wav');
  formData.append('Prompt', '');
  formData.append('JsonHistory', JSON.stringify(
    history.map(m => ({ role: m.role, content: m.content }))
  ));
  formData.append('PatientId', session.patientId ?? '');
  formData.append('PatientName', session.patientName ?? '');

  const res = await fetch(`${API_BASE}/api/atendimento/upload-interacao`, {
    method: 'POST',
    body: formData,
  });

  if (!res.ok) throw new Error(`API error: ${res.status}`);
  // Backend retorna string pura ou JSON — tratar ambos
  const text = await res.text();
  try {
    const json = JSON.parse(text);
    return json.resposta ?? text;
  } catch {
    return text;
  }
}
```

### Sessão do paciente — `src/lib/session.ts`

```typescript
const SESSION_KEY = 'health_concierge_session';

export function saveSession(session: PatientSession): void {
  sessionStorage.setItem(SESSION_KEY, JSON.stringify(session));
}

export function getSession(): PatientSession | null {
  const raw = sessionStorage.getItem(SESSION_KEY);
  if (!raw) return null;
  try { return JSON.parse(raw); } catch { return null; }
}

export function clearSession(): void {
  sessionStorage.removeItem(SESSION_KEY);
}
```

### Referências de arquitetura
- `docs/architecture.md` — Estrutura de pastas, decisões tecnológicas, contratos de API
- `docs/project-brief.md` — Visão geral do produto

### ⚠️ Risco
O endpoint de voz (`upload-interacao`) pode ter bug de `[FromBody]` + `[FromForm]` no backend. Implementar o `sendVoiceMessage` como descrito acima (todos os campos como `FormData`), mas o teste real depende de confirmação do backend.

## Testing
- Não há lógica de negócio nesta story — apenas validação de build e lint
- `npm run build` deve completar sem erros
- `npm run lint` deve completar sem erros

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-11 | 1.0.0 | Story criada | River (@sm) |
